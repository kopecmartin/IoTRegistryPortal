import React from 'react';


export default class PreLoginLayout extends React.Component {

    render() {
        return (
            <div>
                {this.props.children}
            </div>
        );
    }
}
